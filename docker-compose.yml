version: '3.8'
services:
  jp-kana-flashcard-app:
    image: sakanbeer88/jp-kana-flashcard-app:latest
    ports:
      - "3000:3000"
    user: "1001:1001"
    restart: unless-stopped
    depends_on:
      jp-kana-flashcard-app-db:
        condition: service_healthy
    environment:
      - POSTGRES_PRISMA_URL=postgresql://postgres:postgres@jp-kana-flashcard-app-db:5432/postgres?schema=public
      - POSTGRES_URL_NON_POOLING=postgresql://postgres:postgres@jp-kana-flashcard-app-db:5432/postgres?schema=public
      - NEXTAUTH_SECRET=your-secret-key-here
      - NEXTAUTH_URL=http://localhost:3000
      - GITHUB_ID=your-github-client-id
      - GITHUB_SECRET=your-github-client-secret
      - NODE_ENV=development
    command: >
      sh -c "
        npx prisma migrate deploy && npx prisma db seed && npx next start
      "
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

  jp-kana-flashcard-app-db:
    image: postgres:latest
    ports:
      - "7777:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
